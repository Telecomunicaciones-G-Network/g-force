/**
 * CSS media query string for desktop viewports.
 *
 * Used to determine when the application should be displayed in desktop mode.
 * The desktop mode is activated when the viewport width is greater than or equal to 1024px.
 */
export const CHAT_DESKTOP_VIEWPORT = '(width >= 1280px)' as const;
