/**
 * @enum MessageDirections
 *
 * @description Represents the possible directions of a message within the system.
 *
 * @property {string} INCOMING - The message is incoming
 * @property {string} OUTGOING - The message is outgoing
 */
export enum MessageDirections {
  INCOMING = 'INCOMING',
  OUTGOING = 'OUTGOING',
}
