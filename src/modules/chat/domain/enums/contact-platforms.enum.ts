/**
 * @enum ContactPlatforms
 *
 * @description This enum represents the platforms of a contact.
 *
 * @property {string} WHATSAPP - The WhatsApp platform.
 */
export enum ContactPlatforms {
  WHATSAPP = 'WHATSAPP',
}
