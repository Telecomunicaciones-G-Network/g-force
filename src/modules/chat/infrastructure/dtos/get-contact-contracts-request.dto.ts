/**
 * @name GetContactContractsRequestDTO
 *
 * @description This interface represents the request for getting the contact contracts.
 *
 * @property {string} page_size - The page size.
 * @property {string} page - The page number.
 */
export interface GetContactContractsRequestDTO {
  page_size?: string;
  page?: string;
}
